resources:
- repo: self

trigger: none

jobs:
- job: MySQL Regression
  displayName: MySQL Regression

  pool:
    vmImage: 'ubuntu-latest'
  steps:
  - task: UsePythonVersion@0
    displayName: 'Use Python 3.10'
    inputs:
      versionSpec: 3.10
  - bash: |
      git clone https://github.com/Azure/azure-cli.git
      git branch
      cd azure-cli
    displayName: 'git clone azurecli'
  - bash: |
      set -ev
      python -m venv env
      chmod +x env/bin/activate
      . env/bin/activate
      python -m pip install -U pip
      pip install azdev
      azdev --version
      azdev setup . --debug
    displayName: 'azdev setup'
  - bash: |
      set -ev
      . env/bin/activate
      azdev test --live --no-exitfirst test_rdbms_flexible_commands --mark mysql_regression

    displayName: 'azdev test rdbms'